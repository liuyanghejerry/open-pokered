// Refactored from CinnabarPokecenter/script.js to dotzuki-engine-dsl.
// Follows the CeladonPokecenter pattern (Pokecenter nurse dialog
// with @choice + @option). See docs/MIGRATION_NOTES.md §A.

game_scene CinnabarPokecenter {
  @storyline("talkNurse") {
    @trigger(map = "CinnabarPokecenter", npc = 1)
    @speaker("") {
      @t("Welcome to our POKeMON
CENTER!\n\nWe heal your POKeMON back to
perfect health!\n\nShall we heal your POKeMON?", "欢迎光临宝可梦中心！\n\n我们会把你的宝可梦恢复到最佳\n状态！\n\n要为你治疗宝可梦吗？")
    }
    @choice {
      @option(@t("Yes", "是")) {
        @speaker("") {
          @t("OK. We'll need your
POKeMON.", "OK，请把你的宝可梦交给我。")
        }
        faceNpc("1", "left")
        delay(8)
        heal()
        animateHealingMachine()
        faceNpc("1", "down")
        delay(8)
        @speaker("") {
          @t("Thank you!
Your POKeMON are
fighting fit!", "感谢您的耐心等待。\n\n你的宝可梦已完全治愈！")
        }
      }
      @option(@t("No", "否")) {
      }
    }
    @speaker("") {
      @t("We hope to see you again!", "欢迎下次再来！")
    }
  }

  @storyline("talkCooltrainerF") {
    @trigger(map = "CinnabarPokecenter", npc = 2)
    @speaker("") { @t("You can cancel evolution.\n\nWhen a POKEMON is evolving,\nyou can stop it and leave it\nthe way it is.", "你可以取消进化。\n\n当宝可梦进化时，可以停止并保\n持原样。") }
  }

  @storyline("talkGentleman") {
    @trigger(map = "CinnabarPokecenter", npc = 3)
    @speaker("") { @t("Do you have any friends?\n\nPOKEMON you get in trades\ngrow very quickly.\n\nI think it's worth a try!", "你有朋友吗？\n\n通过交换得到的宝可梦会长得非\n常快。\n\n我觉得值得一试！") }
  }

  @storyline("talkLinkReceptionist") {
    @trigger(map = "CinnabarPokecenter", npc = 4)
    @speaker("") { @t("Welcome to the CABLE CLUB!", "欢迎来到联机俱乐部！") }
  }

  // ── Pokémon Center PC (sign 1 at 13,3) ───────────────────────────────
  // engine/menus/pc.asm ActivatePC: BILL's PC / item PC / PROF.OAK's PC.
  @storyline("pcStorage") {
    @trigger(map = "CinnabarPokecenter", sign = 1)
    openPC()
  }


  // ── Bench guy (sign 2 at (0,4)) — PrintBenchGuyText ────────────────────
  // data/events/bench_guys.asm + hidden_events.asm: A on the (0,4) bench
  // tile while facing it; BenchGuyTextPointers requires SPRITE_FACING_LEFT,
  // so gate on the player's facing (getPlayerFacing() -> down/up/left/right).
  @storyline("benchGuy") {
    @trigger(map = "CinnabarPokecenter", sign = 2)
    @if (getPlayerFacing() == "left") {
      @speaker("") {
        @t("#MON can still
learn techniques
after canceling
evolution.

Evolution can wait
until new moves
have been learned.", "即使中途取消进化，\n宝可梦也依然可以\n学习招式。\n\n进化可以等学会\n新的招式之后再进行。")
      }
    }
  }
}
