[package]
name = "scene_apply"
version = "0.1.0"
edition = "2021"
description = "Compile-check a map's script.scene and regenerate its script_config.json from the .scene (story-translation workflow)"

[dependencies]
# jrpg engine crates are consumed via git (see repo README / docs for how to
# switch the engine URL once the real hosting is chosen).
dotzuki-engine-dsl = { git = "https://github.com/liuyanghejerry/dotzuki", rev = "d3d83c470e98d88f9994858693179ee02f3d8904" }
