# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.64.0"
name = "ron"
version = "0.8.1"
authors = [
    "Christopher Durham <cad97@cad97.com>",
    "Dzmitry Malyshau <kvarkus@gmail.com>",
    "Thomas Schaller <torkleyy@gmail.com>",
    "Juniper Tyree <juniper.tyree@helsinki.fi>",
]
description = "Rusty Object Notation"
homepage = "https://github.com/ron-rs/ron"
documentation = "https://docs.rs/ron/"
readme = "README.md"
keywords = [
    "parser",
    "serde",
    "serialization",
]
categories = ["encoding"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/ron-rs/ron"

[dependencies.base64]
version = "0.21"
default-features = false
features = ["alloc"]

[dependencies.bitflags]
version = "2.0"
features = ["serde"]

[dependencies.indexmap]
version = "2.0"
features = ["serde"]
optional = true

# no_std (thumbv4t/GBA port): serde without std, derive + alloc only.
# Hosted consumers see identical behavior (core::error::Error based).
[dependencies.serde]
version = "1.0.60"
default-features = false
features = ["derive", "alloc"]

# truncf/trunc for the float "whole number" formatting check on no_std.
[dependencies.libm]
version = "0.2"

[dependencies.serde_derive]
version = "1.0"

[dev-dependencies.option_set]
version = "0.2"

[dev-dependencies.serde]
version = "1.0"
features = ["derive"]

[dev-dependencies.serde_bytes]
version = "0.11"

[dev-dependencies.serde_json]
version = "1.0"

[features]
default = []
integer128 = []
