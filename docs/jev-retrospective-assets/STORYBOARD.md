## 完整通关双栏

status: outline
src: index.html

单一持续双栏场景，保持 160×144 原始画面的整数 3 倍尺寸。左为脚本（无模型参与），右为策略层 + 动作层 Jev。只有源游戏画面运动，无装饰转场、旁白或音乐。

十章依次覆盖：新开档至图鉴、第一枚徽章、第二枚徽章、三枚徽章、五枚徽章、八枚徽章、首次进入四天王、冠军与名人堂、片尾及自动存档、独立 CONTINUE。第四和第五道馆顺序可能不同，因此合成“五枚徽章”阶段，不重排任何一侧素材。

章节标题、倍速、先完成侧的定格提示始终可读。名人堂及片尾使用 2 倍回放；前面的全路线为 32 倍，完整原片可在交互播放器中低速检查。最后两侧都实际展示独立进程载入的真新镇画面，配合日志验证八徽章与名人堂 1 次。

验收：两条原片帧数分别等于各自原始进程片段之和；章节区间连续且无遗漏；结局证据来自本次实际录像；检查章首、中段、名人堂、片尾和读档的截图、排版与视频导出。


## 同步大盘与关键节点解说（用户已确认）

画布扩展为 1920×1080。左侧仍是两个 480×432 的真实游戏画面；右侧为 Jev 策略层最近选择、候选前三项概率、动作 / 技能执行、已观察结果、队伍状态与调用 / 战败计数。

五个节点各同时停驻两边画面 8 秒：小刚首败、小霞第三次败退、八徽章后选择金牙、三连胜后以 4 HP 撤退、低血量败退后未选补给。停驻使用该时点的真实原片截图；恢复播放时从同一原片位置继续，不删去区间。下方文字明确标注“复盘注解 · 依据日志，非模型自述”。

时间校验：录像采集器与 Jev 原生日志起点差约 3.373 秒，通过 776 个战斗帧锚点核验，零帧不匹配。多个判断落在同一模拟帧时显示最后一项；静止的模型网络等待仍按日志墙钟统计。

独立布局测试已通过 runtime、layout、motion 与 132/132 对比度检查；关键节点、停驻之后继续播放的截图已人工检查。最终两路素材组装后仍需执行完整工程检查与导出验证。

输入观测：右侧 Jev 视频下方上下堆叠两张卡片，分别对应实际策略和动作调用，并以小标题标明仅对应右侧录像；左侧脚本视频下方改为出处说明卡，声明纯脚本运行、无模型参与。队伍/PP、位置、目标、资源、相关路线和重置代价均来自当次 state/question，缺失字段不补造。右侧执行器输入按完成后的下一观测帧对齐，不宣称是精确的按键帧动画。实际编码样片 18 秒与 40 秒已检查文字、HP、输入快照、停驻恢复及像素边缘。
