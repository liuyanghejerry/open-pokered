---
workflow: general-video
flow: automation
storyboard: yes
message: 完整保留两种控制器从真实 NEW GAME 到结局及独立读档的过程，比较路线、准备、失败恢复和成本。
destination: Markdown 技术复盘附件
aspect: 16:9
language: zh-CN
audience: open-pokered 开发者
length: 由完整实机录像按章计算
---

用户明确要求两边都完整通关，不能再用领取图鉴的开场片替代。直接制作并导出，沿用现有纸色背景、双栏布局和静音实机画面。

提供三种观看方式：两条 60 fps 完整原片；按共同剧情进度分章的并排 MP4；支持原片变速、拖动、地图及队伍状态的本地交互播放器。原片包含主游戏进程和明确标记的独立 CONTINUE 进程。

并排版按时间顺序覆盖原片全部区间。通常为 32 倍模拟帧回放，名人堂之后及独立读档为 2 倍；章节内两边倍速相同，先完成的一边定格并显示等候。不得重排路线或删去失败、训练、恢复。加速版会抽取展示帧，完整逐帧证据以原片为准。

同一冻结游戏二进制与 seed 42。旧脚本保留默认运行时钟；Jev 按命令推进。脚本修复新增菜单兼容性，森林败退时提前执行原定 13 级训练，补齐西尔佛劲敌及后续三座道馆败退后的有界恢复，并调整智能战斗治疗阈值及败退重试，标签必须写“脚本（无模型参与）”。Jev 不调用旧 mNN 路线。模型等待不产生模拟帧，用日志统计墙钟时间，不能用并排片长作为竞速结果。

这两条是 2026-09-20 至 21 日的新录制；与 b19 历史通关分开。失败及人为停止的录制分别归档，最终片只使用一个从头到尾固定策略的成功运行，不拼接不同尝试的进度。


用户追加并确认：采用“同步大盘 + 关键节点文字解说”，保持静音。画布扩展至 1920×1080；两路 480×432 实机画面在左侧并排，右侧显示 Jev 最近策略、候选前三项、动作 / 技能执行、实际结果、队伍血量与累计调用。底部显示复盘注解。

以原片游戏帧对齐日志；校正采集器与 Jev 的起始时钟差。关键节点同时暂停两侧 8 秒供阅读；候选概率明确不是成功率，复盘注解明确不是模型自述。另提供可独立打开的 Jev 原片 + 同步大盘播放器。

用户再次追加：展示实际输入而不仅是决策结果。两路画面下方分别显示策略/动作最近一次调用的输入摘要及快照年龄；交互版展开完整 state、指令、全部候选和输出。右侧新增执行器最近输入，标明 A/B/方向序列或 skip_dialogue 等真实调试命令。模型调用、缓存复用、代码保底和一次拒选后的代码条件选择必须区分。不得以当前状态补齐当时未传入的字段，不伪造模型理由。
