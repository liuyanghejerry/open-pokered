for floor,x in [(5,26),(4,24),(3,26),(2,24)]:g.nav_warp(x,0,f'SilphCo{floor}F',f'SilphCo{floor-1}F');g.cutscene()
g.nav_warp(10,17,'SilphCo1F','SaffronCity',approach='down');g.cutscene()
g.tap('start',8);g.tap('a',8)
for _ in range(2):g.tap('down',8)
g.tap('a',8);g.tap('a',8)
for _ in range(9):g.tap('up',8)
snap('retry-fly-indigo');g.tap('a',8);g.cutscene();g.step(180);g.cutscene()
assert g.st()['map_name']=='IndigoPlateau',g.st()
g.nav_warp(9,5,'IndigoPlateau','IndigoPlateauLobby');g.cutscene();checkpoint('m144-retry-league')
