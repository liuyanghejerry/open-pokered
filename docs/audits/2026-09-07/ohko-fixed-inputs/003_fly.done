g.tap('start',8);g.tap('down',8);g.tap('a',8)
for _ in range(2):g.tap('down',8)
g.tap('a',8);g.tap('a',8)
for _ in range(10):g.tap('up',8)
snap('retry-fly-saffron');g.tap('a',8);g.cutscene();g.step(180);g.cutscene()
assert g.st()['map_name']=='SaffronCity',g.st()
checkpoint('m141-retry-saffron')
