# 2026-09-07 审计修复复验（2026-09-08）

验收对象为审计分支的 `7dd44c7` / `86229e2`，以原报告和 A/B/C 台账为范围，重新阅读生产调用链、对照本地 pret/pokered 汇编、执行回归，并合入远端 master `0c32b9d`。此前“全部处理完毕”的总结存在漏修与验证缺口，应以下列复验结果为准。没有把 m01–m10 当成重走冠军流程，也没有把同进程重进地图当成独立读档。

## 发现并补修的项目

| 台账项 | 复验发现 | 补修与证据 |
|---|---|---|
| A03/A04 红莲问答 | 将原版答案字节 FALSE=0 误解为 NO，第一题错判；六门错误使用 GATE0..5；战胜训练家不打开门；对白重新引入字面 #MON | 恢复答案 YES/NO/NO/NO/YES/NO、GATE1..6 和各训练家胜利开门；GATE0 保留给无实体门的第一训练家。六机器实际交互、七训练家胜/负矩阵、答错后战胜开门均覆盖。`cinnabar-before.log` 保留修正断言后旧实现的三项失败 |
| A05/B05 静态宝可梦 | 三鸟和八只电球类对象缺 SRAM toggle 映射；测试仅证明同进程重入；旧档已写完成事件但未写隐藏状态仍会见到对象 | 补 11 个持久化映射，地图加载由完成事件恢复隐藏；`.scene` 和生成配置同时补绑定。`completed_static_encounters_hidden_from_persistent_events` 覆盖全部 11 个对象并检查真实 SRAM 位 |
| A15 劲敌复现 | HIDDEN 优先只是冲突兜底；显隐操作只修改运行态，脚本引擎可能再次回写旧值，随后合法 show 也会受旧 hidden 阻挡 | 显示/隐藏时同时更新两个存储的互斥标记，保留旧档冲突时 HIDDEN 优先。`latest_object_visibility_survives_engine_merge_and_reentry` 连续 show/hide/show/hide；既有劲敌冲突存档用例保留 |
| A18/C20 冠军之路 | 只有玩家掉洞，石头落洞不消失，也未接通二层落石；原版二层石头应在掉落前隐藏 | 三层石头进入 (23,15) 后写事件、隐藏三层对象、显示二层对象，补持久化与 `.scene` 声明；`victory_road_boulder_drops_and_reappears_downstairs` 从持久化事件新建二层场景，验证落点 (23,16)；原有推石压开关和玩家掉洞测试继续通过 |
| A21/B07 PP 道具 | TUI 进入选招后仍调用 TM/HM 完成函数，Ether/Max Ether/PP Up 无效 | core 提供统一 `finish_move_choice`，app/TUI 共用。TUI 实际背包→选队员→选第二招→恢复/提升 PP→扣药，三个道具均覆盖 |
| A13/B01 TUI | 地图仍读静态块，CUT 和门块不更新；饮料仍显示内部常量名 | TUI 渲染使用运行时地图块；饮料转换为本地化显示名。app/TUI 同一状态截图对比，包含真正执行 CUT 的画面 |
| C17 劲敌初始选择 | TUI 仍按当前领队选队伍 | 同 app 使用存档初始选择并保留旧档可推断迁移。`rival_team_uses_saved_starter_after_lead_changes` 以皮卡丘领队、妙蛙种子历史选择验证对应火系劲敌队伍 |
| A12 与 master 合并 | 双方各有一个塔 7F `@load`，对象命名和移动路径不同；机械合并会产生两个加载函数 | 保留 master 的原版三组移动路径与持久化对象名，接入审计分支的战后回调。三人离场/隐藏/重入测试继续覆盖，保留富士老人的绑定 |
| 基线新回归 | master 的中毒结算把领取初始宝可梦前的空队伍当成全队倒下；m03 从卧室步行四步就返回真新镇 | 空队伍直接返回。`empty_party_before_starter_never_blacks_out` 先失败后通过；真实新游戏同按键前后证据、master 同状态渲染对比均归档 |
| 验证工具 | 翻译脚本默认旧路径，实际检查 0 个文件；文档说比对 HEAD，代码却优先 origin/master | 修正为当前 248 个地图，空输入失败；默认 HEAD，新增明确的 `--base`。显式对比 master 会正确报告红莲等本次有意恢复原版的内容变化，不将这类检查伪报为通过 |
| 回归导航 | master 恢复训练家战前对白后，m09 导航器仅发送方向键，停在正常对白而不会确认 | 途中对白交给既有 cutscene 驱动；到达目标坐标时仍把剧情交给里程碑调用者，避免提前处理 m06 劲敌战。路线、里程碑断言、选择题失败规则保持原契约 |

## 其余项目如何验收

A01/A02/A10/A11/A14/A17 的实际渲染及文本路径已核对，并重新用 master 同状态截图替代此前部分非同状态对照；A06 的首个存活队员与经验参与标记、A07/A19/A20/B06 的门禁及战后回调、A08/A09 的正辉电脑与入出机器、A16/B04 的卡比兽捕获/击败分支、B02 的游猎退出旗标均由对应生产代码和完整受影响测试覆盖。

C01–C21 的审计续跑补丁重新核对生产路径与既有测试，包括 OHKO 当前速度、残余伤害不复活、战斗药物按 ItemId 扣减、地下通道与船/联盟出口、钥匙门、正确训练家队伍、劲敌结果变量作用域、塔楼梯、路线阶段和冠军胜利标记。本轮新增 TUI 的 C17 覆盖与 C20 石头关联路径修复；其余没有重复改写。

B03 原始坐标重叠仍未获得独立复现，因此只认定“强制走位期间冻结玩家的保护与交接回归通过”，不把它包装成原问题已实机重现后修复。B08 保留限制：没有保存初始选择、当前领队又不属于初始进化线的旧档，无法可靠恢复历史选择。

## 验证与证据

- `cargo test -p pokered-core`：2540 个核心单元测试通过，连同集成测试共 2863 个通过。
- app / UI / data / debug-server 受影响测试全部通过；TUI 20 个测试通过。
- `python3 scripts/scenarios.py`：10/10 通过。
- 翻译检查：实际检查 248 个场景通过；不存在的目录退出 1；显式 `--base origin/master` 红莲比对按预期检出文本恢复差异。
- 完整新游戏 m01–m10 通过，最终输出 `PLAYTHROUGH REACHED REQUESTED MILESTONE`，在 PewterGym (4,2) 正常取得小刚徽章。此前森林和道馆途中各有一次战败，日志保留为 `forest-blackout.log` / `gym-blackout.log`，重跑通过；空队伍故障与战前对白等待已分别定位修正，不混称为随机抖动。最终运行用 `run-opening-observed.py` 的只读战斗前后日志包装默认完整链，使用本轮构建的独立二进制副本避免其他 cargo 命令覆盖 debug-server 功能，未使用 resume 或状态注入。
- 日志见 [acceptance-logs](acceptance-logs/)。画面见 [master 前后对比](../../screenshots/audit-2026-09-07-acceptance/README.md)，app 与 TUI 各 18 组。原有编译警告保留。
