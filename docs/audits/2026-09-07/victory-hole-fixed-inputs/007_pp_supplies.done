g.nav_warp(7,11,'IndigoPlateauLobby','IndigoPlateau',approach='down');g.cutscene();g.step(90)
g.tap('start',8);g.tap('down',8);g.tap('a',8);g.tap('a',8);g.tap('a',8)
for _ in range(3):g.tap('up',8)
snap('pp-supply-fly-cerulean');g.tap('a',8);g.cutscene();g.step(180);g.cutscene()
assert g.st()['map_name']=='CeruleanCity',g.st()
g.nav_to(20,0,'CeruleanCity');g.d.drive(['up']*16,frames=32);g.cutscene();g.step(30)
assert g.st()['map_name']=='Route24',g.st()
g.nav_to(19,8,'Route24');g.d.drive(['right']*16,frames=32);g.cutscene();g.step(30)
assert g.st()['map_name']=='Route25',g.st()
g.nav_to(10,2,'Route25');g.face('up');g.tap('a',12);g.cutscene();g.step(30);snap('route25-elixer-found')
g.nav_to(38,4,'Route25');g.face('up');g.tap('a',12);g.cutscene();g.step(30);snap('route25-ether-found')
checkpoint('m122-league-pp-supplies')
g.tap('start',8);g.tap('a',8);g.tap('a',8);g.tap('a',8)
for _ in range(9):g.tap('up',8)
snap('pp-supply-fly-indigo');g.tap('a',8);g.cutscene();g.step(180);g.cutscene()
assert g.st()['map_name']=='IndigoPlateau',g.st();checkpoint('m123-league-ready')
