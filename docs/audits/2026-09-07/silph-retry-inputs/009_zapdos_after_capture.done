g.tap('a',20);snap('caught-zapdos-still-cries');g.cutscene();g.step(30)
assert g.st()['screen']=='overworld',g.st()
assert g.d.cmd(cmd='get_npcs')['data'][8]['visible'],g.st()
g.nav_warp(0,11,'PowerPlant','Route10',approach='left');g.cutscene();g.step(40)
checkpoint('m66-power-plant-exit')
g.tap('start',8);g.tap('a',8);g.tap('down',8);g.tap('down',8);g.tap('a',8);g.tap('a',8)
for _ in range(8):g.tap('up',8)
snap('fly-saffron-after-zapdos');g.tap('a',8);g.cutscene();g.step(180);g.cutscene()
assert g.st()['map_name']=='SaffronCity',g.st()
checkpoint('m66-saffron-with-zapdos')
