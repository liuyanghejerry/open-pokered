# Jev 自主探索：追齐一周目

最新验证结果（2026-09-20）：**连续真实 NEW GAME → 八徽章 → 冠军 → 名人堂 → 片尾 → 游戏自动存档 → 独立进程 CONTINUE 已完整通过。** 首次成功的新开档为 `.artifacts/jev-autonomous-fresh/20260920-081028-seed42`（b19），未从开发检查点恢复，未使用原 playthrough 的里程碑处理器或既有路线。独立读档落在真新镇，徽章位图 255、名人堂记录 1。281 项相关 Python 测试通过。

该次运行耗时 **4926.469 秒（82 分 6 秒）**，Jev `jev-1.13.0` 策略判断 383 次、动作判断 2033 次，执行 1081 个操作，推进 1045823 帧。记录 16 次战败：小霞 5 次、科拿 10 次、渡 1 次。最终喷火龙 95 级、拉普拉斯 15 级。完成目标不代表效率或全局最优：满血后的过度恢复仍多次出现，也不能由单一 seed 的一次成功推断稳定成功率。

| 证据 | 结果 |
| --- | --- |
| 起点 | `RedsHouse2F`，空队伍、零徽章、名人堂 0 |
| 八徽章完成 | 2726.689 秒 |
| 完整结局及独立读档完成 | 4926.462 秒 |
| 独立 CONTINUE | `PalletTown` / `overworld` / badges 255 / HOF 1 |
| 模型输入 | 策略 2189612 + 动作 7799443 tokens |
| 模型输出 | 137967 tokens |
| 策略 SHA-256 | `672ebec9b5eb383295df88309aeed2facc741c0864271a1be0b0b9aab69b027f` |
| 二进制 SHA-256 | `c34978a8bcd1a75212e9f590b2fe131fdff7377a4c744de7c63c74beb9b2c978` |
| 结局自动存档 SHA-256 | `a0c9a55e337ae3358e138d0eee7d43188f0193ffdc07690beca4e90423dc0107` |

运行目录保留 `summary.json`、`trace.jsonl`、`final-observations.json` 和 `verification-audit.json`。[审计摘要](audits/2026-09-20/jev-autonomous-seed42.json) 随代码提交，完整逐步记录保留在本地忽略目录。审计确认当前策略逐文件哈希与启动版本相同，游戏过程命令未使用 warp、give、set_flag 或 restore；HOF、TheEnd、返回标题及独立 CONTINUE 均有观察记录。`--checkpoint` 在上述验证之后额外保存开发现场，所以目录中的 `game.sav` 是验证后的开发存档，并非上述自动存档原始字节；两者哈希分别记在审计文件中。

以下按时间保留开发记录；历史段落中的“仍在验证”描述当时状态，当前结论以上述连续新开档审计为准。

本次已达到约定的原 playthrough 一周目终点：真实 NEW GAME、八徽章、冠军、名人堂、片尾、游戏自动存档及独立进程 CONTINUE 保留记录。原 m01–m49 仅作为终点覆盖参照，不作为执行路线。

用户明确选择：**坚持自主探索，只复用通用操作技能，不按既有路线推进。** 不调用 `mNN` 里程碑函数或按其顺序调度。脚本 AST、地图连接、公开训练家与宝可梦数据可作为白盒观察；Jev 负责目标及操作判断，代码执行参数化导航、战斗与菜单技能。

当前开发入口：

```bash
python3 -u scripts/openpokered/run_autonomous.py --until beat-brock --checkpoint
# 从本运行器真实游玩获得的开发检查点继续，不加载旧 playthrough 通关存档
python3 -u scripts/openpokered/run_autonomous.py --resume .artifacts/jev-autonomous/<run> --until beat-misty --checkpoint
# 完整新开档验证命令；b19 已通过
python3 -u scripts/openpokered/run_autonomous.py --until become-champion \
  --output .artifacts/jev-autonomous-fresh --max-calls 4000 \
  --max-actions 4000 --wall-budget 10800 --checkpoint
```

`--until` 用于定位开发中的阻碍，不改变最终追齐目标。最终须 fresh 验证，续跑证据单独保留。

已接入：

- 独立二进制/附属存档与真实开机、默认命名。
- 对实际调试命令做白名单审计，拒绝 warp、give_*、set_flag、restore_state。开发检查点只在停止后另行保存。
- 原条件保持的剧情目标图；新增依据待战对手等级和已到访草地生成训练候选，依据脚本中的 heal() 效果生成治疗候选。
- 复用通用按键/寻路/战斗技能，Jev 选择可用攻击。招式判断缓存保留 HP、PP 是否不足、禁用状态和对手等条件。
- 每轮完整 trace、终态观察、失败 traceback、命令计数及可选开发检查点。

开发续跑已正常击败小刚：`target/agent/runs/autonomous/20260919-194424-seed42`，起点来自本运行器真实 NEW GAME 链。没有强化队伍或剧情注入；训练、治疗与挑战时机由两层判断组合决定。该段 44 次操作，策略 16 次、动作 67 次 Jev 判断，114.456 秒；终态妙蛙种子 15 级、1 枚徽章。包含旅途内部代码处理的 4 场战斗，不能声称每场战斗均由 Jev 决策。新增柜台交谈与当前可达草地区域检测，均有针对性测试。

`become-champion` 已改成名人堂、片尾、游戏自动存档及独立进程 CONTINUE 的持久化证据，瞬时冠军 flag 不再计为通关；终点驱动现已在开发续跑中验证，fresh 证据另行记录。

月见山至小霞开发续跑通过：`target/agent/runs/autonomous/20260919-195620-seed42`。Jev 根据实际受阻反馈选择研究员、化石，再回到道馆目标；18 次操作，策略 8 次、动作 39 次判断，81.167 秒。逐格跨图寻路代替原自动旅行器，保留真实室外入口上下文；途中攻击使用 Jev 判断（含缓存），没有调用旧里程碑。

此后开放全部剩余目标，`20260919-200412-seed42` 自主回溯挡路 NPC 的显示条件，完成正辉细胞分离、船票、圣安奴号劲敌和 HM01。235.656 秒后因尚无学习/清障候选而停止，仍只有两徽章；该运行保留了多次真实战败和升级，不能算作全链成功。现已补充从标准地形和 HM 数据生成的学习/砍树技能，正在续跑验证。

`20260919-201007-seed42` 已通过真实 HM 菜单学习居合斩，并击败马志士取得第三枚徽章；队伍自然升级、进化为 32 级妙蛙花。之后卡在 12 号道路。发现并修复：洞穴内部传送地面被错误限制到地图边缘；已砍树木在重新入图恢复后，导航缓存仍认定已清除。两者均有回归测试；绕行的实际验证仍在继续。

`20260919-202553-seed42` 已穿过岩山隧道、到达玉虹市并击败莉佳，取得第四枚徽章。该段 75 次操作，策略 31 次、动作 100 次 Jev 判断，293.076 秒；终态妙蛙花 39 级。随后根据受阻反馈自主寻找宝可梦之笛，在富士老人被隐藏时停止。修复了探索器遗漏 SRAM 默认隐藏对象的问题，同时用当前可见 NPC 校验本地图候选。期间还修正了 PP 恢复判断、途中战斗结束后的重新规划与实际移动的失败计数。

这些均为开发续跑证据。部分旧检查点在战斗中停止，保存的是队伍进入战斗时的数据；续跑不能替代连续 fresh 通关证据。运行摘要现记录检查点画面状态，最终验证不得依赖调试保存。

后续开发运行自主购买了清水。发现并修复了两类规划问题：明确受阻时立即回到策略层，清障候选排除无关地面道具；位于地图边缘的出口地毯同样必须向外走才能传送，不能把横向移动视为离开电梯（地图内部的显式关卡入口仍可进入）。普通寻路也不再把电梯当作无需操作的跨层通道。修复时主动中断的开发运行均标记为未成功，继续使用本运行器获得的检查点。

`20260919-205534-seed42` 已完成塔内三名六层训练家及三名七层火箭队成员，拾取了阻挡通路的神奇糖果，救出富士老人并获得宝可梦之笛。道具球是否影响通路来自忽略碰撞后的候选路径与实际物体坐标交集，普通可选道具不再笼统视为前置任务。只读战斗状态新增 `is_ghost`，动作层可以选择逃离无法攻击的未识别幽灵；剧情幽灵同时将识别道具需求传回策略层。

该段还暴露了游戏行为差异：未获得检视镜、未设置 `EVENT_BEAT_GHOST_MAROWAK`，仍通过真实移动进入七层并救出富士老人。保留原始 trace；不能把这段记成完成检视镜/幽灵任务，也不修改游戏状态来掩盖差异。

上述 `205534` 的剧情事件有 trace 证据，但主动中断发生在协议读写之间，最终观察响应错位，复制到的存档并未保留最新进展。该检查点不作为可复用证据。运行器现在只在完整命令往返结束后响应中断，并验证最终观察类型及保存确认；不符合条件的检查点拒绝续跑。

`20260919-210938-seed42` 已重新完成富士老人、宝可梦之笛及 12 号道路卡比兽，并实际击败 Koga，取得第五枚徽章。该段 51 次操作，策略 35 次、动作 103 次 Jev 判断，446.158 秒；最终妙蛙花 47 级，检查点位于浅红市，最终协议观察有效。随后因路线不断经过自行车关卡而停止，未达到全链目标。

寻路现保留已观察到的通行阻碍，按对应脚本的实际条件临时避开强制退回坐标；获得通行道具后自动解除限制。障碍坐标来自脚本配置，没有增加地图行程表。策略判断同时能看到此前目标的寻路失败记录，动作结果区分“其他训练家战斗已完成”和“预期剧情效果已发生”。

尚未完成：第五枚徽章之后的完整自主推进、更多钥匙/HM/机关的通用处理、全链稳定性及从全新游戏开始的最终验证。最近一次完整相关测试为 201 项 Python、13 项 Rust semantics 通过；其后关卡绕行和前置道具解除限制的专项 37 项通过。

`20260919-211851-seed42` 的续跑已绕开自行车关卡，领取兑换券并兑换自行车，抵达金黄市及西尔佛公司。其后因门禁开合未参与几何规划而重复受阻；新增脚本 `replaceTileBlock` 效果、钥匙前置条件和固体交互点操作，以及基于水域几何的冲浪需求、兼容队员和 HM 学习候选。新增能力尚待实跑；最新相关 Python 测试 204 项通过。

开发中发现整个 `target/` 目录已不存在，具体原因未确认。上述各轮的详细 trace 和检查点因此当前不可用，运行摘要已在本会话临时文件清理时归档至 `.artifacts/jev-session-20260919/temporary-logs.tar.gz`（含 `jev-autonomous-full-a*.log`）。不能用摘要冒充完整可复查证据。后续运行产物改存 `.artifacts/jev-autonomous/`，与编译目录分离；重建后从真实 NEW GAME 重新验证。

重建后的新开局 `.artifacts/jev-autonomous/20260919-212811-seed42` 自主选了小火龙，在 226.028 秒实际击败小刚；自然进化为火恐龙，穿过月见山到达华蓝市。小霞战反复失败，610.367 秒在操作决策间隙安全停止，最终 27 级、1 枚徽章，检查点位于华蓝市且脚本空闲、协议观察有效。不能把此前已丢失的五徽章检查点与此次新开局混作同一条连续证据。

据此新增战败反馈：保留实际对手及失败等级，向策略层提供进一步训练的候选；训练仍是正常战斗，未改等级。当前测试为 206 项 Python、重建后的 13 项 Rust semantics 通过。门禁交互的动作候选也覆盖了“先设置开门标志、再替换门块”的脚本结构；这些改动在开发续跑中继续验证。

`.artifacts/jev-autonomous/20260919-213904-seed42` 实际击败小霞，并完成正辉与船票；停止时火恐龙 33 级、两徽章。发现登船规划把数据中的 `ShipPort` 与引擎的 `ship_port` 当成不同名称，漏掉跳板传送；已统一名称并加入真实地图回归测试。`.artifacts/jev-autonomous/20260919-214738-seed42` 随后取得 HM01、通过菜单学习居合斩、实际击败马志士，恢复到三徽章；97.341 秒后因绕行候选不足停止。

第三徽章之后发现，实际 NPC 占路分支过早返回，遗漏了可以通过居合斩到达的绕道。修复后同时提供 NPC 前置条件及可达树木，`20260919-215111-seed42` 已实际从 12 号道路绕行至 9 号道路并进入岩山隧道。相同 NPC/脚本交互的不同预测效果现在共享重试额度，防止给每条条件分支重复分配失败预算。当前这些新路径仍在开发验证，不代表全新游戏已完成一周目。

该次续跑在 225.768 秒实际击败莉佳，达到四徽章。之后补齐跨地图静止 NPC 障碍记忆、旋转地板和电梯菜单；脚本只读取的普通训练家胜利标志，从引擎公开的 `trainer_headers.rs` 与地图 NPC 顺序建立生产规则，未引用旧里程碑路线。

恢复测试的 `20260919-223645-seed42` 在 120.477 秒实际击败掉落钥匙的训练家，125.376 秒取得 LIFT KEY，138.515 秒通过真实菜单选择电梯楼层，154.931 秒取得基地开门标志。仍为四徽章，尚未取得透视镜。保留完整 trace、SRAM 和动态标志；这些是开发续跑证据，最终仍须全新 NEW GAME 至独立进程 CONTINUE 验证。

策略候选保留前置条件的因果说明，战斗准备仅列对应 NPC 的队伍。新增目标交互区域的几何预览，区分同图可达区域与受阻区域；有可达候选时暂缓已在真实执行中受阻且目前仍无路径的目标。当前 75 项相关 Python 测试通过；这项候选筛选仍在实跑验证。后续控制台日志写入 `.artifacts/jev-session-20260919/`，不再积累 `/tmp` 日志。

`20260919-224740-seed42` 在 10.346 秒实际击败基地坂木，13.144 秒取得透视镜。通用 NPC 交互改为观察当前位置、导航到邻格、面向并按 A；途中遭遇战结束后先重新规划。战败分支中的强制退回也不再误判为挑战前不可通过的地形。

`20260919-225029-seed42` 在 18.971 秒实际击败幽灵嘎啦嘎啦，32.109 秒救出富士老人，38.570 秒取得宝可梦之笛，214.426 秒击败阿桔取得第五徽章，318.105 秒兑换自行车。随后根据门禁受阻反馈自主寻找钥匙卡，进入西尔佛公司五层；370.871 秒在操作判断间隙停止，以加载通用剧情传送候选的改进。新能力按脚本实际落点验证到目标的步行路径，支持付费入口等跨区域传送，保留真实菜单和付款前置条件。79 项相关 Python 测试通过，实跑验证继续；仍未完成全新开局的冠军与独立 CONTINUE 全链。

`20260919-225858-seed42` 在 39.687 秒取得钥匙卡，随后打开三层两道门；`20260919-230201-seed42` 在 54.104 秒实际击败公司内的劲敌，105.621 秒击败十一层守卫。仍为五徽章，尚未击败公司坂木。修正了只凭楼层名称反复乘电梯的问题，传送落点必须有通向目标区域的具体路径；多目标共用一次 BFS，避免逐坐标重复全图搜索。

进一步发现引擎为门、雕像和答题机提供了脚本坐标元数据之外的 OnInteract 范围。规划器现读取这些公开绑定，区分真实按 A 交互与 OnStep 踩踏触发，并能从十一层门外侧生成开门动作。治疗候选也保留已观察到的锁门障碍，不因 HP/PP 变化重新尝试同一不可达护士。83 项相关 Python 测试通过，加载这些修正继续实跑；不据单元测试声称剧情已完成。

`20260919-231121-seed42` 在 25.571 秒从门外侧实际使用钥匙卡开门，39.681 秒击败西尔佛公司坂木，60.773 秒确认第六徽章（娜姿）。随后在自行车道路因按键时长不匹配而往返，最终 `strategy:no_selection`；有效检查点保存于道路上。通用导航现按观察到的 Biking/Walking 选择每格 4/8 帧，新增一格转弯不越过目标的回归测试，84 项相关 Python 测试通过。`20260919-231421-seed42` 已正常继续进入 17 号道路，向狩猎区入口推进。

17 号道路实跑暴露了两项额外操作条件：中性帧会触发自动下坡，B 才是刹车；逆坡骑行取消加速。更底层的 `press_timeline` 入队与 `step_frames` 两次请求之间还可能由 driven-only 循环执行输入，产生多余松键帧。新增向后兼容的 `advance:true` 同步输入模式，动作驱动验证返回帧差，禁止遗留输入队列干扰新输入。游戏侧测试验证连续输入帧数准确且无后台剩余输入；调试协议 5 项、完整相关 Python 226 项通过。新调试二进制 SHA-256 为 `c34978a8bcd1a75212e9f590b2fe131fdff7377a4c744de7c63c74beb9b2c978`。

`20260919-232657-seed42` 从六徽章检查点重新出发，22.668 秒经正常按键穿过自行车道路并付费进入狩猎区，实际获得 30 个狩猎球和入场标志。随后为治疗离场时，菜单判断选择 none 并停止；当时脚本仍处于离场选择，未将该检查点作为稳定续跑起点。菜单状态现增加真实旅行目的地，明确途中关卡选择可以间接推进最终目标。87 项专项测试通过，重新从六徽章稳定检查点验证。此前越界的道路运行仅保留为失败证据。

`20260919-233013-seed42` 正常取得 HM03、拉普拉斯，并通过菜单学习冲浪。双子岛水流暴露了旧通用水路技能不支持传送的断言错误；改为有界、闭环的跨图水路导航，战斗和强制位移作为结果反馈。剧情绝对移动 `movePlayer` 同时加入障碍条件分析。

`20260919-234201-seed42` 自主从真新镇方向经 21 号水路抵达红莲岛，143.774 秒击败夏伯，172.777 秒击败常青道馆坂木，取得八徽章。该段实际解开红莲道馆六道门；在常青市宝可梦中心操作边界停止，29 次操作，最终观察有效。没有调用原 playthrough 路线。水路搜索还读取引擎强制骑车入口的步行连通区域，排除实际禁止冲浪的自行车道路捷径。

推石机关生产规则现来自引擎的机关/洞口表及落层条件；通用搜索按当前石块、墙体、地形高度与出口方向求解，不包含手写推法。地图测试验证完整推法及数据不被修改，纠正了把可横向站立的出口地毯完全禁用的问题。该能力尚待真实机关完成验证。

`20260919-234707-seed42` / `234930` 发现 20 格背包已满，金牙交互没有产生道具；没有将预测效果当成完成。`235146` 由 Jev 选择给拉普拉斯使用最后一颗神奇糖果，通过实际菜单升至 16 级并腾出空位。准备动作与脚本生产目标仍由策略层选择。已完成父目标的旧受阻支线不再持续生成任务；远处最终目标尚被锁住时，导航会分段发现通向中间区域的水路需求。

当前新增推石/水路等完整相关测试 231 项 Python 通过；其后背包和目标链改进的 91 项专项通过。控制台及失败证据继续统一保留在 `.artifacts/jev-session-20260919/` 与 `.artifacts/jev-autonomous/`。

后续实跑发现 `tools/map_data.json` 的联盟关卡出口落后于当前 `map.json`：游戏北门明确通往 23 号道路，旧导出仍是 LAST_MAP。规划器改为读取与构建相同的地图块及门口数据，保留导出中已解码的块集。通过真实移动进入 23 号道路和冠军之路，未修改游戏地图。

背包准备还需考虑旅途新收到的道具：重新入园领取狩猎球后，金牙仍因容量限制无法取得。加入兼容 TM 的正常学习候选、途中背包容量变化触发重规划、当前菜单技能可达性；`20260920-000836-seed42` 实际取得金牙、HM04，并让喷火龙学会怪力。Jev 为腾出空间先选择 Toxic 替换 Flamethrower，再用 Strength 替换 Toxic；这个招式取舍并非已证明最优，保留原始证据。

导航障碍按目标及受阻地点分别记忆，续跑从本实验 trace 恢复旧版本曾观察到的障碍；不读取旧 playthrough 的路线。成功发现新地图或穿过新的水路会更新导航知识版本，使已被解开的旅行失败不再永久封锁目标。`000836` 在渡过 23 号道路后因此前两个不同水路共享失败计数而停止，这项修正正在新一轮验证。

独立 fresh 实验 `.artifacts/jev-autonomous-fresh/20260919-235923-seed42` 从真实 NEW GAME 开始，273.124 秒击败小刚；其后长期在低等级的 1 号道路训练，主动停止，**未完成 fresh 通关验证**。训练候选现允许已探索地图的相邻区域，按公开野生等级和经验数据提供比较，避免只能反复使用起点草丛。新增相关测试通过；最终 fresh 验证仍待后续执行。

`20260920-001234-seed42` 根据地图搜索生成 18 次推石，最后一步未观察到移动便停止报告成功；重试后在 11.518 秒实际确认冠军之路 1F 开关标志。跨区域路径分析随后加入可还原的地图机关预测，寻找潜在道路所需的脚本条件；仅改变规划副本，不修改游戏。传送入口也必须实际可达，避免把墙后的落洞当作通往该墙的捷径。

`20260920-002131-seed42` 实际解开 2F 两个开关，64.571 秒确认第二个开关。74.195 秒在普通训练师战中落败，正常训练至 62 级后停止。攻击记录显示 Jev 连续选择怪力而非高暴击的劈斩，动作输入现补充引擎公式下的暴击概率及期望威力；数值明确不包含实时攻防、能力等级或次要效果。学习菜单候选保留同属性较强攻击，策略层可选择用已有 TM 改善弱攻击。未观察到 NPC 挡路时，不再仅因同图存在隐藏条件就生成击败传说宝可梦的支线。

相关 Python 回归 239 项通过。后续策略提示将训练、治疗及招式准备明确视为间接推进；可选招式准备不会挤掉所有尚待探索的剧情目标。续跑验证仍在继续。

`20260920-003035-seed42` 实际使用已有 TM38，将火焰旋涡替换为大字爆。水路可达性预览、进入地图后的自动移动与禁止回退的出口限制分别处理，防止把单向出口误当成不能进入的墙。已有寻路失败的目标在策略判断前重新分析当前地图条件，不要求 Jev 先重复选择无法直接到达的最终目标。

`20260920-003852-seed42` 通过商店真实菜单购买两份全复药；`004101` 实际在野外使用药品并在冠军之路正常训练至 65 级。训练地形现按引擎规则包含洞窟普通地板，并排除出口与水面。道具库存、价格、兼容性和状态治疗条件来自公开数据；不注入道具或金钱。

`20260920-004501-seed42` 实际击败科拿和希巴，到达菊子。大字爆 PP 耗尽，普通系招式无法命中耿鬼，Jev 拒绝无效攻击并停止在战斗菜单；**这一战斗中的检查点不作为稳定续跑点**。后续补充已有 TM 的属性覆盖比较，以及无有效攻击时的正常换人候选；若所有合法攻击均无效，只通过正常战斗结算失败，不伪造胜利。最新稳定开发检查点为 `004101` 的石英高原大厅，队伍 65/16 级，仍未通关。

独立 fresh `20260920-004309-seed42` 因训练目的地被剧情门禁挡住而反复旅行，主动停止，未完成通关。训练失败现在返回原始阻挡结果，已知不可达的训练地点不再持续推荐。升级学招由动作层判断是否接受及替换哪个招式；不会仅因新招有伤害就自动覆盖旧招。

Jev 的 `none` 与相似操作会分摊选择概率：当多个合法候选的总概率超过 `none` 时，程序在这些候选中取模型最高概率项，并单独记录条件选择；明确拒绝和服务失败仍独立报告。策略输入仅保留候选相关标志与失败，完整世界事实仍用于条件求值。相关 Python 回归现为 245 项通过。冠军、名人堂、片尾和独立 CONTINUE 验证仍未完成。


`20260920-005530-seed42` 正常出售金珠、使用已有 TM28 学会挖洞，并在 100.045 秒击败菊子。之后的联盟尝试因连续战斗消耗落败；恢复条件记录实际出战 HP、PP、招式和药品，而非单纯提高等级。若此前到达对手时主力已严重受伤且没有药品，可达商店的补给成为新的准备条件。

`20260920-010542-seed42` 在真实商店购买全复药后，实际通过渡；首次冠军战仍失败，继续正常补给和挑战，最终在 161.007 秒确认完整终点。名人堂序列、片尾 TheEnd、返回标题及独立进程 CONTINUE 均有观察记录；独立读档显示 PalletTown、badges=255、hall_of_fame_count=1。自动存档 SHA-256 为 `2a00306c00a56569bef87cf93593cf84a3a36ba175f50f4d5a2f7b3ba15e3360`。该段策略调用 25 次、动作调用 97 次，模型为 jev-1.13.0，没有里程碑处理器；它从开发检查点开始，**不是 fresh 全链成功**。

独立 fresh `20260920-005841-seed42` 于 312.499 秒击败小刚，经过月见山到达华蓝道馆；487.499 秒普通训练家战败后，导航仍重试原 NPC，触发地图断言而停止。根因是“战斗结束，交回策略层”的中断继承了局部寻路错误，被接近 NPC 的重试捕获。现将两种结果分开，并用真实通用接近函数验证战败中断不会重试或继续按 A。训练目的地的阻挡也统一写入导航记忆。

新增 PP 恢复候选根据道具效果和实测耗尽记录生成，只有已到访区域的实际道具生产规则可参与；使用通过真实背包菜单，尚待独立实跑验证。相关 Python 测试现为 250 项通过。新一轮 fresh `20260920-011405-seed42` 从真实 NEW GAME 启动，运行中不加载开发存档、不调用调试保存。


fresh `20260920-011405-seed42` 发现训练候选与执行条件不一致：主力 HP 满但 Scratch 仅 8/35 PP，训练技能按恢复阈值立即退出，策略却反复选择继续训练。主动停止这一失败轮；训练候选现在使用与执行器相同的恢复条件，新增回归同时验证补充 PP 前不可选训练、恢复后可选训练。251 项相关测试通过。完整 fresh 验证重新启动于 `20260920-012112-seed42`，结果待观察。


fresh `20260920-012112-seed42` 于 186.944 秒击败小刚，正常到达华蓝道馆并承受两次真实战败，导航中断未再崩溃。最终因训练动作拒选、随后没有可推进目标而停止。重放日志显示：5/9 号道路的已知阻挡在训练规划时漏计当前 NPC 碰撞，24 号道路则没有把已知可达证据传给动作层。现训练规划同时检查当前 NPC、选择可达草地区域并向两层提供路径信息；从失败轮最终观察重建，得到 24 号道路 70 步、4 号道路 39 步的可行训练路径，受阻的 5/9 号道路不再列出。252 项相关测试通过。

新 fresh 轮为 `20260920-013314-seed42`。启用停止后的开发检查点保存以便定位后续问题；通关证明仍必须来自停止前的名人堂自动存档和独立进程 CONTINUE，不能使用开发检查点制造成功。


fresh `20260920-013314-seed42` 在 229.030 秒击败小刚，华蓝道馆战败后仍于 437.805 秒停止。此时 24/4 号道路可达性已正确传入，但动作层仍以 none=0.58 拒绝策略层刚选中的训练目标。现进一步明确层间职责：策略层可决定是否训练；队伍满足执行条件且候选均有实际地形路径时，动作层只在可达训练地点中排序，不再重复否决策略决定。没有已知可达路径或需要恢复时，拒选仍保留；服务失败和无效回复依然停止。253 项测试通过。

开发续跑 `20260920-014313-seed42` 从上述华蓝市空闲检查点验证，独立 fresh `20260920-014315-seed42` 同时从 NEW GAME 验证相同策略版本。两者分别记录，开发恢复不充当完整新开局证据。


`20260920-014313-seed42` 在 335.857 秒实际击败小霞；392.385 秒又因出城候选缺少因果前提而停止。当前位置在华蓝宝可梦中心，已记住的城内守卫导致跨地图寻路失败，但旧反馈错误地只记当前位置和 Cut/Surf 猜测。新增跨地图已观察 NPC 的路径差分：仅在保留 NPC 时无路、规划副本移除 NPC 时有路的情况下，记录路径上的第一个实际阻挡者及其地图，再按该 NPC 的脚本显示条件回溯前置剧情。不会把所有 NPC 或地图中的支线都当作必须完成。

254 项测试通过。开发续跑 `20260920-015258-seed42` 已据此选择正辉的细胞分离剧情，正常通过 24/25 号道路并自然学会劈斩。fresh `20260920-014315-seed42` 使用旧策略，已在华蓝市策略边界停止保留记录；等待开发验证稳定后固定版本再做完整 fresh 重跑。


`20260920-015258-seed42` 实际完成正辉、船票、华蓝火箭队员、TM28 和 HM01，但清障旅行的受阻没有进入统一导航记忆，反复选择远处树木。现统一记录清障/冲浪接近旅行的阻挡，前置分析也保留尚未完成的训练与地形目标。

`20260920-015954-seed42` 正常绕行 9 号道路、穿过岩山隧道，254.274 秒击败莉佳、402.844 秒击败马志士，达到四徽章。随后发现把普通训练家的旧迎战位置当成跨地图永久障碍会妨碍回访；非当前地图不再使用这类位置作为确定碰撞，入图后仍按真实 NPC 碰撞导航。剧情守卫和当前地图 NPC 的约束保留。256 项完整相关测试通过；有效空闲检查点位于 Route5，继续验证于 `20260920-021239-seed42`。

招式替换另做实际 Jev 对照：保持 `015258` 学居合斩时的状态与候选不变，原通用动作问题选择忘掉挖洞（概率 0.54）；加入保留战斗能力、属性覆盖和高暴击攻击的要求后，三次均选择忘掉瞪眼（各 0.97）。已用于学招动作，尚不能据单一状态的对照声称完整通关效率提升。原始对照记录为 `.artifacts/jev-session-20260919/move-replacement-probe.json`。


`20260920-021239-seed42` / `021518` 未取得新剧情进展。独立读取各自正常保存的检查点，复现了复合阻挡：直达宝可梦塔的地形搜索同时受 NPC 与树木约束，整条路径未能给出 Cut 候选，但通向中间区域的搜索能发现 Route9 的树木。现沿地图连接分段寻找确实尚不可达的前沿，保留 NPC 因果反馈并优先返回可执行的地形前提；不内置指定路线。257 项相关 Python 测试通过。开发续跑 `20260920-022246-seed42` 已实际生成 Route9 居合斩候选，后续执行与剧情进展待验证。

`20260920-022246-seed42` 于 19.344 秒实际砍开 Route9 的树木，但随后仍过早接受远端 NPC 依赖。现优先探索真实可达的中间区域，再回退到远处 NPC 前提；258 项相关测试通过。`20260920-022454-seed42` 已实际穿过岩山隧道、抵达宝可梦塔六层，56.157 秒选择当地幽灵事件，消除了此前四徽章检查点的原地循环。继续验证后续前置任务。

`20260920-022454-seed42` 的塔内受阻进一步暴露了前置任务不能递归展开的问题。导航依赖现从仍然有效的目标递归展开到稳定状态，包含 HM/兼容队员准备，并仍排除无关或已完成支线。259 项相关测试通过。`20260920-022923-seed42` 已于 15.353 秒拾取堵路神奇糖果，74.419 秒救出富士老人，79.803 秒取得宝可梦之笛，102.466 秒完成卡比兽战斗。该轮未确认击败幽灵便可到达七层的游戏行为与早期观察一致，如实保留，不声称检视镜或幽灵任务完成。开始相同策略版本的独立 fresh b08，开发续跑另行记录。

`20260920-022923-seed42` 在 229.797 秒实际击败阿桔，260.588 秒取得 HM03；随后在 Route8 再次停滞。地图高层路由省略守卫小屋，导致历史守卫阻挡被误当作无关；现结合路线地图的真实传送口保留这类依赖，259 项回归通过。独立 fresh b08 (`20260920-023141-seed42`) 于 251.435 秒击败小刚，后因加载同一修复主动停止，未完成完整验证。

`20260920-023905-seed42` 于 68.360 秒实际交付饮料解开金黄市门禁，之后因钥匙卡接近点选错而停止。最终观察和真实地图复核证明：左侧接近点有 46 步路径，右侧被当前训练家挡住；策略预览计入了实时 NPC，旅行执行的候选接近点选择却漏计，导致反复选择更近但不可达的一侧。现旅行规划统一加入当前 NPC 碰撞，真实地图回归及完整相关 260 项测试通过。后续续跑验证继续。

`20260920-024639-seed42` 已于 18.776 秒取得钥匙卡，45.649 秒击败公司劲敌，94.936 秒确认娜姿徽章（第六枚），129.131 秒领取拉普拉斯，随后正常学习冲浪。该轮抵达 20 号水路后在空闲步行状态停止。水路实跑暴露了效率问题：放宽水面后的最短路径反复采用可步行绕过的小捷径；现选择沿途较远且陆路可达的岸边，并要求落脚点不在已有陆路连通区域中。已在真实地图验证可达岸边和不可步行到达的对岸，261 项相关 Python 测试通过；新水路规划仍待续跑验证。

`20260920-030007-seed42` 到达红莲岛并正常治疗，随后实际取得金牙与 HM04，163.583 秒让喷火龙用怪力替换瞪眼。285.010 秒在双子岛 B2F 因没有可执行目标停止：当地石块仍隐藏，旧引擎推石规则漏掉了上层落石前提。现按几何搜索匹配能推入目标洞口的配置石块，把其可见性加入生产规则，再利用现有脚本显示条件递归回溯上层事件；未写入指定推法。262 项相关 Python 测试通过，继续从有效空闲检查点实跑。

`20260920-031010-seed42` 于 81.350 秒实际完成一层第一块落石。随后把机关接近点限制到几何上能完成该目标的石块周围，263 项测试通过。`20260920-031230-seed42` 于 37.514 秒完成 B1F 第一块、121.073 秒完成 B2F 第一块、132.508 秒完成一层第二块落石。下层多步机关则多次意外落层：推石求解器避开洞口，但普通站位导航会从洞口穿过。现机关的旅行接近点与每次站位移动使用相同洞口限制，真实地图测试验证绕行和临时限制恢复，264 项相关测试通过。仍为六徽章，未完成 fresh 全链验证。

`20260920-032246-seed42` 于 43.418 秒实际完成 B3F 第二块落石，81.070 秒完成 B1F 第二块落石，证明洞口避让已生效。随后发现进入同层另一片不连通区域时仍会直接尝试推石；动作候选现先验证石块区域的本地可达性，必要时重新跨图接近。路径前提分析也会在规划副本中放宽已观察到的脚本退回坐标，并反推解除其分支条件所需的真实剧情效果；执行碰撞保持不变。266 项相关 Python 测试通过，继续于 `20260920-033122-seed42` 验证。

`20260920-033122-seed42` 已从红莲道馆的实测退回条件反推出秘密钥匙。随后修正规划过度放宽：先保留脚本门禁寻找路径，仅在无路时放宽，以免可绕过的捷径生成伪前提。`20260920-033309-seed42` 实际抵达宝可梦屋二楼，但逐门放宽把互斥布局拼成不可能路线，反复切换后策略拒绝继续。

新增通用联动机关搜索：从实际脚本提取可逆 flag 与全部受其控制的门块，在地图副本中搜索开关及脚本传送的可达序列，不改变引擎状态。开发探测发现 `getPlayerFacing` 未被识别，三处雕像被当作未知前提；现把朝向作为动作条件保留，并让接近和实际交互遵循方向要求。269 项相关测试通过；只读探测 `.artifacts/jev-session-20260919/mechanism-probe.json` 找到了通往秘密钥匙的开关/跳落序列。

`20260920-034523-seed42` 实际触发三楼开关，但地形改变后父目标从普通前沿消失，策略又撤销开关。现保留联动机关的父目标直到物品实际取得，操作后仍从真实状态重算；专项 68 项通过，开发续跑于 `20260920-034756-seed42` 启动。独立 fresh b09 为 `.artifacts/jev-autonomous-fresh/20260920-033351-seed42`，245.479 秒击败小刚；它加载的是联动机关修复前的策略版本，不能作为新修复的最终验证。

`20260920-035033-seed42` 于 93.327 秒实际取得秘密钥匙。现已覆盖联动机关完成后的物品交接、退出区域、跳落坐标分支约束，以及开发检查点的父目标保存。只有玩家能分别设置和复位的开关才参与该搜索，入图自动重置的推石事件不当作可逆开关。`20260920-035444-seed42` 实际走出宝可梦屋，96.517 秒击败夏伯、131.759 秒击败坂木，达到八徽章；后在冠军之路暴露“当前位置已是训练地点”的单节点路径格式错误。

独立 fresh b09 在两徽章后陷入 Route11 与洞口往返。局部避草偏好在换图后反向；现对离图又回到本图的避草候选使用普通碰撞寻路。恢复验证 `.artifacts/jev-autonomous-recovery/20260920-040253-seed42` 于 20.114 秒实际抵达 Route12，打破原循环。原 b09 已暂停，不能记为完整通关。修复训练单节点路径后，273 项相关 Python 测试通过，新的独立 fresh b10 和联盟开发续跑 a94 已启动；当前仍未取得 fresh 全链成功证据。

独立 fresh b10 为 `.artifacts/jev-autonomous-fresh/20260920-040434-seed42`，已于 282.730 秒击败小刚、807.395 秒击败小霞。开发续跑 a94 (`20260920-040436-seed42`) 已实际击败科拿、希巴和菊子，正在挑战渡；期间记录了真实战败、回入口治疗及重复挑战，效率仍有明显改进空间。

恢复 c01 (`20260920-040253-seed42`) 在卡比兽消失后又出现 Route12/Lavender 往返。独立真实存档观察证明实时道路可达；旧固定 NPC 位置却仍留在历史巡逻障碍集合。现实时观察会清除固定 NPC 移动或隐藏后的旧占位，游走 NPC 的巡逻记录仍保留。274 项完整相关测试通过。c02 (`20260920-041458-seed42`) 已正常抵达浅红道馆并于 127.706 秒击败阿桔。b10 启动在这项缓存修复之前，其策略指纹和验证边界单独保留。

开发续跑 a94 (`20260920-040436-seed42`) 已在 1395.654 秒完成冠军、名人堂、片尾及独立进程 CONTINUE 验证：真新镇、八徽章、名人堂 1，喷火龙 97 级、拉普拉斯 15 级，自动存档 SHA-256 `c5bfd8bf1b3334f58b9ec93b64bd54dda2d9870711713617f31b2eaeda52051b`。期间遗忘劈开后的低效配招依靠多次真实训练和重战完成；此轮策略 156 次、动作 1108 次判断，不能据此声称效率最优。

独立 fresh b10 于 1002.079 秒取得第三徽章，随后完成富士老人及卡比兽事件，但重现已修复的固定 NPC 历史占位问题，安全停止并保留现场。最新策略启动新的独立 fresh b11；恢复 c02 已于 302.815 秒击败莉佳、491.326 秒击败娜姿，取得冲浪并抵达红莲岛。最终 fresh 全链验证仍未完成。

恢复 c02 暴露了新问题：尚未取得机关内物品时，列表靠前的外部道馆目标先生成离开机关候选，导致进出循环。机制规划现先比较可达计划，优先保留机关内部尚未完成的前置目标，再考虑为外部目标退出；已完成的物品目标及时释放。新增内外目标同时存在的回归测试，275 项完整相关 Python 测试通过。c03 (`20260920-043130-seed42`) 从真实空闲存档继续验证；b11 在约两分钟时安全停止以加载修复，不能算作通关。新的独立 fresh b12 已启动。

恢复 c03 于 220.923 秒真实取得秘密钥匙。其后策略选中了可达的退出开关，动作判断却因缺少父目标和机关计划而拒绝，最终 `strategy:no_selection`。在相同失败状态中补充策略上下文，Jev 三次均选择正确操作（0.68、0.60、0.59；原记录为拒绝 0.59），见 `.artifacts/jev-session-20260919/mechanism-context-probe.json`。现动作层接收所选策略的上下文；对策略已经选定且实时几何确认可达的机关步骤，动作层选择执行操作，不重新否决该策略，未确认可达的动作仍可拒绝。276 项相关 Python 测试通过，恢复 c04 从该空闲真实存档继续。独立 fresh b12 (`20260920-043238-seed42`) 于 309.409 秒实际击败小刚；它启动在动作上下文改动之前，后续验证按实际策略指纹区分。

恢复 c04 (`20260920-043821-seed42`) 已实际完成离开宝可梦屋，并于 103.844 秒击败夏伯取得第七徽章，验证机关父目标与动作上下文修复的连续执行。后续联盟验证继续。

当前版本独立 fresh b13 (`20260920-044353-seed42`) 已从 NEW GAME 连续取得四徽章：小刚 270.258 秒、小霞 761.947 秒、马志士 963.909 秒、阿桔 1278.504 秒。b12 独立连续运行也已达五徽章，并取得拉普拉斯和冲浪；它选择先去红莲岛，尚未击败娜姿。

恢复 c04 在 134.495 秒取得八徽章，之后真实取得金牙和 HM04，把怪力教给拉普拉斯，保留喷火龙的劈开、居合斩、喷射火焰、挖洞。已连续完成双子岛及冠军之路的通用机关，进入四天王。实际记录显示，击败菊子后挖洞 PP 耗尽，策略反复选择回入口治疗，入图脚本又重置已胜四天王；这是未计入剧情损失的恢复决策问题，不能误记为菊子战力不足。新增治疗路线的已胜战斗重置代价，来自真实 @load resetFlag 和胜利生产规则，未写死地图或路线。受控 Jev 对照增加代价后有 1/3 次选择继续（另两次仍选治疗），仅据此不能声称已消除循环，后续恢复实跑验证。

恢复 c05 (`20260920-050802-seed42`) 已验证最新策略的完整收尾：先返回治疗重打一轮，随后在挖洞 PP 为零时选择继续，以劈开和喷射火焰击败渡和冠军；56.358 秒内完成名人堂、片尾、游戏自动存档及独立进程 CONTINUE。读档为真新镇、八徽章、名人堂 1，喷火龙 94 级、拉普拉斯 15 级。策略 9 次、动作 41 次判断；自动存档 SHA-256 `63c535209ea4b3f021f8882d17fcce09444d1f749d712d477e5b89c32c3c2133`，策略 SHA-256 `d1c2ff370c3a8d9418e1e481e4f6c6abd09624eecbb73f7d352b773681e9e9dd`。277 项相关 Python 测试通过，启动相同版本的独立 fresh b14；不能把这次恢复结果当作 fresh 完整验证。

独立 fresh b13 已从真实 NEW GAME 不间断取得八徽章：秘密钥匙 2183.775 秒、夏伯 2360.344 秒、坂木 2400.728 秒；未用开发检查点恢复。该轮包含机关父目标与动作上下文修复，启动早于治疗路线重置代价的改动，后续联盟及存档结果仍待验证。最新策略独立 fresh b14 (`20260920-051001-seed42`) 已于 215.758 秒击败小刚、863.536 秒击败小霞。较旧 b12 在五徽章和宝可梦屋阶段主动安全停止，未完成通关。

独立 fresh b13 在 2598.783 秒以 `strategy:no_selection` 停止，完成八徽章但没有完成联盟。该停止不是先前四天王 PP 问题：在 CinnabarIsland(19,4) 到后续地区的水面规划中，最短路从地图边缘陆地直接跨入下一张地图的水格，现有冲浪启动技能无法执行，而原搜索没有考虑替代岸边。独立真实存档副本比较证明有无障碍记忆均出现此问题，排除了水路历史缓存；新水面规划排除这种不可执行起步，仍允许水到水跨图和跨图登陆。真实 CinnabarIsland→PalletTown 回归与 278 项相关 Python 测试通过，恢复 c06 从该现场验证。b14 仍在连续实跑，启动早于这项边界起步修复。

恢复 c06 (`20260920-053252-seed42`) 已实际从红莲岛北上、通过冠军之路、完成四天王和冠军，并验证名人堂、片尾、自动存档与独立 CONTINUE。耗时 724.404 秒，策略 83 次、动作 435 次判断，最终喷火龙 79 级、拉普拉斯 15 级；读档为真新镇、八徽章、名人堂 1。该轮同时覆盖冲浪边界修复和治疗重置代价，仍是恢复验证。最新策略的新开档 b15 (`20260920-053352-seed42`) 已于 226.526 秒击败小刚；b14 连续六徽章并已到宝可梦屋三层，两轮 fresh 都尚未完成。

独立 fresh b14 连续取得八徽章、通过冠军之路，随后在联盟反复返回治疗；喷火龙自然升到 100 级，仍在满血、仅少量 PP 消耗时退回。它曾连续击败科拿、希巴和菊子，但又重置进度，最终在策略边界安全停止，不能算作 fresh 通关。只读对照显示，删去自动入图候选仍 3/3 选择治疗；按父目标合并后 2/3 选择继续，见 `.artifacts/jev-session-20260919/recovery-goal-grouping-probe.json`，仅作为候选粒度问题的线索，未直接改动运行策略。

进一步以两个真实失败状态（满血但 PP 略有消耗、击败前三位后挖洞 PP 耗尽）各做三次只读判断，明确“准备不要求每战补满、启发式紧急标记不是强制指令、比较当前可用招式是否足够”后，6/6 选择继续；见 `.artifacts/jev-session-20260919/recovery-resource-sufficiency-probe.json`。已加入该通用说明，仍允许 Jev 在确实无法继续时撤退；没有写入联盟地图或固定选择。278 项相关 Python 测试再次通过。

恢复 c07 (`20260920-062452-seed42`) 从 b14 的真实现场验证了该修正：39.771 秒、策略 6 次/动作 28 次判断，连续通过余下联盟并完成名人堂、片尾及独立 CONTINUE；真新镇、八徽章、名人堂 1。自动存档 SHA-256 `dd84322ab2d9d2bfd22d33cd54359e4223ba89f9278ffd2d3c537655226468da`，策略 SHA-256 `de5329915a41321156d5cd1c007f8de492cc849177d50a73d5117834db1caa59`。旧提示版本的独立 fresh b15 已取得八徽章并进入联盟，新提示版本启动独立 fresh b16；两者证据分别记录。

独立 fresh b15 (`20260920-053352-seed42`) 同样连续取得八徽章、通过冠军之路，但重现旧恢复提示下的过度补给，在策略边界安全停止；它不计为完整通关。后续验证集中于新恢复提示的独立 fresh b16 (`20260920-062617-seed42`)，该轮已于 235.369 秒击败小刚、831.115 秒击败小霞，并真实获得居合斩。

独立 fresh b16 已从 NEW GAME 连续拿齐八徽章（阿桔 1342.523 秒、莉佳 1515.797 秒、娜姿 1704.531 秒、夏伯 2289.496 秒、坂木 2328.891 秒），真实取得冲浪、怪力并进入冠军之路。2587.814 秒附近推石被野战打断后重新规划，通用寻路探索到西尔佛电梯的静态占位出口 `UnusedMapED`，其目标入口表为空，触发 `IndexError`。该轮停止且最终观察有效，仍未完成 fresh 通关。

修复 `warp_edges_from`，按游戏引擎 `resolve_warp_destination` 的语义拒绝没有有效落点的静态边，不把电梯占位出口当捷径。真实电梯地图回归及 279 项相关 Python 测试通过。恢复 c08 从 b16 的真实现场验证；独立 fresh b17 重新验证完整流程，两者分别保留证据。

恢复 c08 (`20260920-071154-seed42`) 已实际重新解开冠军之路机关，完成联盟、名人堂、片尾及独立 CONTINUE。耗时 1469.853 秒，策略 153 次、动作 867 次判断；从喷火龙 58 级继续，最终 90 级、拉普拉斯 16 级。此段记录 18 次战败，说明新提示支持连续推进，但战斗和准备效率仍不足。读档为真新镇、八徽章、名人堂 1。策略 SHA-256 `14f20fdca0002c14273eb3eda7c3f2e8564ffd7870e1becaf9b4d230ea1bb29c`，自动存档 SHA-256 `b1b03e4fe8f3cda7122d2d31dcbfc900e8cc642c9b42a10dd561ed5d45cd581f`。相同代码版本的独立 fresh b17 (`20260920-071156-seed42`) 已取得第五徽章，完整 fresh 验证仍在进行。

独立 fresh b17 (`20260920-071156-seed42`) 连续取得八徽章，在 2819.454 秒以 `strategy:no_candidates` 停止，喷火龙 57 级，尚未取得金牙和怪力。失败现场的独立只读寻路比较排除了 NPC 缓存与陆路不通：陆路能到达浅红市，但狩猎入口的拒绝选项会后退，被错误解释成无条件墙，阻断付款交互候选。放松障碍后最短水路又引出双子岛怪力前提，形成金牙与怪力循环。

现按真实脚本分支判断：同一剧情触发器存在当前可满足的另一选项且产生传送时，不把拒绝分支的后退当作无条件墙。付款与入场仍必须由真实对话执行，不写死入口地图或绕行路线。增加钱够、钱不足、无关传送不能解锁的回归；280 项相关 Python 测试通过（13.563 秒），`git diff --check` 干净。现场对照恢复了付款入口的可达性和候选；恢复 c09 (`20260920-080616-seed42`) 已实际选择金牙目标，沿陆路向东推进，同时启动独立 fresh b18 验证全链。

恢复 c09 已取得金牙，但 81.507 秒在退出狩猎区时停止：出口确认会修改入口守卫所读的标志，再自动移动，也被误判为墙。进一步允许同一剧情中当前可执行的对话分支解除自身守卫；只有无关标志写入时不放松障碍。新增正反例后，281 项相关 Python 测试通过（13.625 秒）。恢复 c10 (`20260920-080945-seed42`) 已实际退出狩猎区、交还金牙并取得怪力；b18 在早期训练阶段安全停止，完整验证改由包括两项分支修正的独立 fresh b19 承担。

恢复 c10 (`20260920-080945-seed42`) 已完成冠军、名人堂、片尾、游戏自动存档及独立 CONTINUE。1112.588 秒，策略 96 次、动作 494 次判断，策略输入 448227 / 动作输入 2630408 / 输出 29553 tokens；记录 11 次战败。最终喷火龙 76 级、HP 32/246、劈开 PP 2、喷射火焰和挖洞 PP 0，拉普拉斯 18 级。低剩余资源下仍连续完成最后挑战，随后真实读档为真新镇、八徽章、名人堂 1。策略 SHA-256 `672ebec9b5eb383295df88309aeed2facc741c0864271a1be0b0b9aab69b027f`；自动存档 SHA-256 `25a4e5ee4e3edd7647d709a97ad4f7979532d58b75002149bd76e3d03eef5021`。逐文件哈希确认运行策略与当前代码一致，未把恢复链拼接为 fresh 成功。相同版本的独立 fresh b19 (`20260920-081028-seed42`) 持续运行，已取得两枚徽章。

独立 fresh b19 已从 NEW GAME 连续拿齐八徽章（夏伯 2683.356 秒、坂木 2726.689 秒），完成金牙、怪力、冠军之路并进入联盟。狩猎区入场与退出修复已实际覆盖。联盟仍有明显过度治疗：83 级满血时也曾返回护士，不能声称恢复循环已经消除。只读战略意图分组对照 3/3 选择继续，结果保存于 `b19-intent-grouping-probe.json`；未热改运行策略，完整 fresh 结局仍待验证。

独立 fresh b19 最终于 4926.469 秒成功结束。最后一轮连续完成四天王与冠军，在挖洞 PP 0 时继续使用劈开和喷射火焰；最终 HP 244/324、劈开 PP 9、喷射火焰 PP 3。独立 CONTINUE 验证真新镇、八徽章、名人堂 1；所有本次自主运行进程均已正常结束。最新证据和限制见本文顶部，早期恢复结果不用于拼接这条连续验证链。
